// Módulo Mesa por Elas Club — gestão de assinantes.
// AINDA NÃO IMPLEMENTADO. Este arquivo existe como esqueleto para a próxima
// etapa do projeto (cadastro de assinantes, status de adesão, etc.).
//
// Quando for implementado, este módulo deve seguir o mesmo padrão dos módulos
// de Eventos: funções de comunicação com o backend (ver sales-data.js como
// referência) + funções de renderização de UI.

export function initClubSubscribers(){
  // TODO: buscar assinantes (aba ClubAssinantes no backend) e renderizar.
}
