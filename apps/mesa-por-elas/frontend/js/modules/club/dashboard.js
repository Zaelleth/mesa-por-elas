// Módulo Mesa por Elas Club — dashboard de assinaturas.
// AINDA NÃO IMPLEMENTADO. Este arquivo existe como esqueleto para a próxima
// etapa do projeto (receita recorrente mensal, inadimplência, etc.).
//
// Quando for implementado, este módulo deve seguir o mesmo padrão do
// dashboard de Eventos (ver modules/eventos/dashboard.js como referência).

export function initClubDashboard(){
  // TODO: renderizar receita recorrente, assinantes ativos, inadimplência.
}
